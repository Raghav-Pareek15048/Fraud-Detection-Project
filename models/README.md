## Trained Model

The trained machine learning model is stored in the `models/` directory.

- `best_fraud_model.pkl` – Trained fraud detection model.
- `scaler.pkl` – Feature scaler used during preprocessing (if applicable).

These files can be loaded using `joblib` for making predictions.
